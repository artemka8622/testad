### Переход в папку с .csproj файлом
- `cd ./Arm.CreateTask`

### Установка npm-пакетов
- `npm install`

## Работа с приложением

### Сборка приложения
- `dotnet build` (запустит в том числе webpack для js)

### Запуск приложения
- `dotnet run`